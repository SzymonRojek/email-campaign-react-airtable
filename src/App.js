import { BrowserRouter as Router } from "react-router-dom";

import MainContainer from "./pages/MainContainer";

const App = () => (
  <Router>
    <MainContainer />
  </Router>
);

export default App;
